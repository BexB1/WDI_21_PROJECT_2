<br />  

<small><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.username : 'Guest' %></strong>
  <% if a.trackable %>
    added the pledge <%= link_to a.trackable.name, pledge_path(a.trackable) %>.
  <% else %>
    added a pledge which is now deleted.
  <% end %>